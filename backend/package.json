{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "deploy:mock": "hardhat run scripts/deploy-mock.js --network sepolia",
    "deploy:fhe": "hardhat run scripts/deploy.js --network fhevmTestnet",
    "test": "hardhat test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "fhevm": "^0.6.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.1",
    "ethers": "^6.15.0",
    "hardhat": "^2.26.3"
  }
}
